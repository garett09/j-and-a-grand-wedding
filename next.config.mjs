/** @type {import('next').NextConfig} */
const nextConfig = {
    experimental: {
        // v4 might not need specific experimental flags in Next 15 but keeping it clean
    },
};

export default nextConfig;
